# CooCoo 產品語言與 MVP 邊界

本檔是程式、介面、資料庫與測試共同使用的產品語言。遇到同義詞時，以這裡的定義為準。

## 核心對象

- **料理次數**：一次完整開始並完成料理的事件。一次煮兩份仍只算 1 次。
- **餐份**：料理產出的可獨立食用份數。每份只能是已吃或熟食庫存之一。
- **自煮餐數**：使用者實際吃掉的自煮餐份數。煮好但尚未吃的餐份不先計入。
- **熟食庫存**：已完成料理但尚未吃掉的餐份；它不是原始食材庫存。
- **省下金額**：使用者設定的本人外食比較價，減去本餐實際使用食材成本；最低為 0。
- **圓夢入帳**：使用者看過差額並確認後，新增的不可覆寫金額事件。計算省下金額不等於已入帳。
- **食材重疊率**：本週不含常備調味料的食材種類中，被至少兩餐使用的種類比例。
- **庫存覆蓋率**：規劃餐點的不含常備調味料食材需求中，已由現有庫存覆蓋的需求比例；與食材重疊率分開顯示。
- **即期食材**：依食安與保存規則，接近建議使用期限、應提高使用優先度的庫存批次。
- **低體力餐**：含備料在 30 分鐘內、最多 6 個主要步驟、使用 1–2 個鍋具的料理。
- **快手餐**：總時間真正不超過 15 分鐘的料理；不得只因是低體力餐就顯示此標籤。
- **料理包**：開始料理前下載至裝置的完整文字食譜、步驟、計時、安全提醒、語音文字及圖片索引。

## 不可違反的產品規則

1. 過敏與禁食是硬限制，任何推薦都不得以偏好或節省為理由放寬。
2. AI/OCR 失敗要清楚失敗；正式環境不得回傳模擬成功。
3. 料理文字包未成功存入 IndexedDB 前，不得宣稱可離線料理。
4. 料理完成、庫存扣除與圓夢入帳都以唯一操作 ID 防止重複。
5. 料理次數、自煮餐數、餐份與熟食庫存不得混用。
6. 健康成果只呈現自煮餐數與實際吃過的不同蔬菜種類，不推算鈉、脂肪或醫療效果。
7. 開發者工具只存在開發版本；邀請管理是受角色保護的正式管理功能。
8. 舊資料只可由使用者預覽後主動匯入，不自動覆蓋或刪除。

## 第一版使用情境

主要使用者是小資族、租屋族與外食族。核心閉環是：設定小目標 → 決定本週／今日餐點 → 採買 → 入庫 → 離線料理 → 確認省下金額 → 更新圓夢與健康進度。首輪為 10–30 位受邀測試者，驗收裝置包含 iPhone Safari 與 Android Chrome。

## 已確認的版本策略

- 正式產品只維護一個整合版，以 `main` 的 TypeScript 共用契約、核心規則與測試作為骨架。
- 選擇性移植 `legacy-720-development` 的手機卡片、主廚相談室、圓夢通行證、料理步驟與慶祝回饋；不帶入分散的假資料或全域接線。
- `main` 與 `legacy-720-development` 原分支都保留作比較與復原，不以整批合併覆寫。
- 手機底部固定五頁：今日、採買、冰箱、廚房、圓夢。頁面元件與 CSS 應保持隔離，避免單頁微調波及其他已確認頁面。
