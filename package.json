{
  "name": "stitch_coocoo_1.0_renew",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --cwd apps/web build && bun run --cwd apps/api typecheck",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "bun run --cwd apps/web lint",
    "test": "bun test packages/core apps/api apps/web/src",
    "verify": "bun run lint && bun run format:check && bun run test && bun run build && bun run --cwd apps/web test:e2e",
    "dev": "bun run --cwd apps/api dev && bun run --cwd apps/web dev -- --host 127.0.0.1"
  },
  "msw": {
    "workerDirectory": [
      "apps/web/public"
    ]
  },
  "devDependencies": {
    "@electric-sql/pglite": "0.5.8",
    "prettier": "^3.9.6"
  }
}
