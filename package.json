{
  "name": "stitch_coocoo_1.0_renew",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --cwd apps/web build && bun run --cwd apps/api typecheck",
    "lint": "bun run --cwd apps/web lint",
    "test": "bun test packages/core apps/api apps/web",
    "verify": "bun run lint && bun run test && bun run build"
  },
  "msw": {
    "workerDirectory": [
      "apps/web/public"
    ]
  }
}
