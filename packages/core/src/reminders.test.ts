import{describe,expect,test}from"bun:test";import{canSendReminder,type ReminderDelivery}from"./reminders";const p={expiringIngredients:true,plannedMeals:true,weeklyRhythm:true,pushEnabled:true,quietHoursStart:"21:00",quietHoursEnd:"09:00",weeklyLimit:3 as const};const week="2026-09-07";describe("smart reminders",()=>{test("blocks quiet hours",()=>expect(canSendReminder("expiring",p,[],new Date("2026-09-11T14:00:00Z"),week)).toBeFalse());test("allows each kind once and at most three per week",()=>{const history:ReminderDelivery[]=[{kind:"expiring",sentAt:"",weekStart:week},{kind:"planned",sentAt:"",weekStart:week}];expect(canSendReminder("expiring",p,history,new Date("2026-09-11T04:00:00Z"),week)).toBeFalse();expect(canSendReminder("rhythm",p,history,new Date("2026-09-11T04:00:00Z"),week)).toBeTrue();expect(canSendReminder("rhythm",p,[...history,{kind:"rhythm",sentAt:"",weekStart:week}],new Date("2026-09-11T04:00:00Z"),week)).toBeFalse()});test("requires explicit push permission",()=>expect(canSendReminder("expiring",{...p,pushEnabled:false},[],new Date("2026-09-11T04:00:00Z"),week)).toBeFalse())});
